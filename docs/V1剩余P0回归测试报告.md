# CodeCoachAI V1 剩余 P0 回归测试报告

## 1. 测试环境

- 测试日期：2026-05-14
- 工作区：`C:\my-claude`
- 前端仓库：`C:\my-claude\CodeCoachAI-vue`
- 后端仓库：`C:\my-claude\CodeCoachAI-java`
- 文档仓库：`C:\my-claude\CodeCoachAI-doc`
- 前端访问地址：`http://127.0.0.1:5173`
- 后端 Gateway：`http://127.0.0.1:8080`
- 测试方式：普通用户浏览器回归测试，辅以 Gateway 接口核对、前端请求代码核对和后端日志关键字检查。

## 2. 测试账号

| 角色 | username | password |
| --- | --- | --- |
| 管理员 | `e2e_admin` | `E2eAdmin@123` |
| 普通用户 | `e2e_user` | `E2eUser@123` |

本轮实际浏览器回归使用普通用户 `e2e_user`。

## 3. 后端服务状态

- Nacos 正常监听：`8848`。
- Gateway 正常监听：`8080`。
- CodeCoachAI 后端服务端口均在监听：`9201` 到 `9207`。
- 前端 Vite 开发服务已启动并监听：`5173`。
- 普通用户登录接口、用户题库接口、面试历史接口、面试报告接口均可通过 Gateway 正常返回。

## 4. 用户端题库标签筛选测试结果

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 题库列表能正常加载 | 通过 | 浏览器进入 `/questions`，题库列表显示 `Total 21`。 |
| 标签筛选项能正常显示 | 通过 | 标签筛选项包含 `E2E_TEST_缓存`。 |
| 选择 `E2E_TEST_缓存` | 通过 | 标签下拉可正常选择该项。 |
| 页面不再出现 `Total 0` | 通过 | 选择后页面显示 `Total 3`。 |
| 返回正确题目 | 通过 | 返回 3 条缓存相关题目，均展示 `E2E_TEST_缓存` 标签。 |
| `/questions` 传递真实 `tagId` | 通过 | Gateway 直测 `GET /questions?pageNo=1&pageSize=10&tagId=100002` 返回 3 条，返回数据包含真实 `tagIds: [100002]`。 |
| 不传 `tagIds` | 通过 | `getQuestionsApi` 解构剔除 `tagIds`，请求参数只保留 `tagId`。 |
| 不调用 `/admin/question-tags` | 通过 | 用户题库页未引入该 API；后端日志关键字未检出本轮用户流程调用。 |
| 不调用 `/admin/question-categories` | 通过 | 用户题库页未引入该 API；后端日志关键字未检出本轮用户流程调用。 |
| 不展示禁用题 | 通过 | Gateway 直测 `/questions?pageNo=1&pageSize=100` 返回 `total=21`，状态统计为 `status=1 count=21`；浏览器列表未出现禁用状态。 |
| 页面无业务 404 | 通过 | 浏览器页面流程无 404 提示；浏览器控制台和后端日志关键字检查未发现业务 404。 |

结论：用户端题库标签筛选 P0 已通过。

## 5. 面试报告中文化测试结果

测试报告页：`/interviews/100005/report`

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 面试报告能正常打开 | 通过 | 浏览器可打开报告页，显示综合得分 `82`。 |
| 不出现 `Mock report` | 通过 | 页面未出现 `Mock report`。 |
| 不出现旧英文句子 | 通过 | 页面未出现 `Mock report: the interview has been completed`。 |
| AI 总结为中文 | 通过 | 显示“本场 V1 模拟面试已完成...”等中文内容。 |
| 回答亮点为中文 | 通过 | 显示“回答亮点：能够围绕 Java 后端常见题目...”等中文内容。 |
| 主要问题为中文 | 通过 | 显示“主要问题：部分回答停留在结论层...”等中文内容。 |
| 复习建议为中文 | 通过 | 显示“复习建议：1. 复盘集合、并发...”等中文内容。 |
| 总分来源说明可见 | 通过 | 显示“总分来源于后端面试报告 totalScore 字段...”说明。 |
| 阶段得分空状态合理 | 通过 | 显示“暂无阶段得分数据”。 |
| 不展示 V2 功能 | 通过 | 未看到学习计划、AI 简历优化等 V2 功能。 |
| 页面无业务 404 | 通过 | 浏览器页面流程无 404 提示。 |

结论：面试报告 Mock report 中文化 P0 已通过。

## 6. Network 异常请求清单

本轮浏览器业务流程、前端请求代码和后端日志关键字检查均未发现以下异常：

- 业务 404
- `/inner/**`
- 旧 `/ai/interview/**`
- `questions/answer`
- `questions/wrongs`
- 分类 / 标签 / 问题组 `/status` 子路径
- 用户端误调 `/admin/**`

补充说明：

- 前端用户题库页只通过 `/questions` 获取列表，并从列表数据生成分类和标签筛选项。
- 前端用户题库请求已剔除 `tagIds`，只传 `tagId`。
- 浏览器控制台关键字检查结果为空。
- 后端日志关键字检查未检出 `/inner/`、`/ai/interview`、`questions/answer`、`questions/wrongs`、用户端误调分类/标签/问题组 admin 路径等记录。

## 7. 是否仍有 `tagIds` 误传

否。

依据：

- `getQuestionsApi` 使用 `const { tagIds: _tagIds, ...restParams } = params` 剔除 `tagIds`。
- `requestParams` 只显式设置 `tagId: params.tagId`。
- 浏览器选择 `E2E_TEST_缓存` 后页面返回 `Total 3`，与 Gateway 直测 `tagId=100002` 结果一致。

## 8. 是否仍有用户端误调 `/admin/**`

否。

依据：

- 用户题库页未调用 `/admin/question-tags`、`/admin/question-categories`。
- 本轮浏览器流程、控制台关键字检查、后端日志关键字检查均未发现用户端触发 `/admin/**`。

## 9. 是否仍有 `/inner/**`

否。

本轮浏览器业务流程未发现 `/inner/**` 请求。

## 10. 是否仍有旧 `/ai/interview/**`

否。

本轮浏览器业务流程未发现旧 `/ai/interview/**` 请求。

## 11. 是否仍出现英文 `Mock report`

否。

浏览器报告页未出现 `Mock report` 或 `Mock report: the interview has been completed`，报告核心内容已中文化。

## 12. 是否仍阻塞 V1 演示

不阻塞。

两个剩余 P0 阻塞项均已通过最终回归：

- 用户端题库标签筛选：通过。
- 面试报告 Mock report 中文化：通过。

## 13. 下一步建议

1. V1 演示前保留当前数据库演示数据，不再调整接口和页面范围。
2. 演示前只做一次快速冒烟：登录普通用户、题库筛选 `E2E_TEST_缓存`、打开 `/interviews/100005/report`。
3. 当前不建议继续扩展 V2/V3 功能或新增接口，避免引入新的演示风险。
