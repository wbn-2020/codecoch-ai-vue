# V2 前端浏览器真实全链路回归报告

## 1. 测试环境

- 前端仓库：`C:\my-claude\CodeCoachAI-vue`
- 前端分支：`dev-ui`
- 最新 commit：`e5828a9`
- 前端启动地址：`http://127.0.0.1:5173`
- 后端地址：`http://localhost:8080`
- 前端环境配置：`.env.development` 中 `VITE_API_BASE_URL=http://localhost:8080`
- 测试时间：2026-05-16 21:50 - 22:08（Asia/Shanghai）
- 浏览器：Codex in-app browser（Chromium）
- 普通用户账号类型：`e2e_user`（未记录密码）
- 管理员账号类型：`e2e_admin`（未记录密码）
- 初始工作区状态：`git status -sb` 显示干净
- 初始构建：`npm run build` 通过，仅有 Rollup 注释 warning 与 chunk size warning，本轮按要求未处理
- lint/test 脚本：`package.json` 未提供 `lint` / `test` 脚本，未执行

## 2. 总体结论

- 是否连接真实后端：是，登录接口 `POST http://localhost:8080/auth/login` 对普通用户和管理员均返回真实 token。
- 是否使用真实账号：是，使用 `e2e_user` 与 `e2e_admin`。
- 是否执行浏览器真实测试：是，已在浏览器中完成登录、退出、路由跳转、提交、确认弹窗、面试作答、后台页面访问和部分后台 CRUD。
- P0 阻断：0 个。
- 是否可以进入下一阶段：建议先修复 P1 问题后再进入下一阶段展示验收；若只看主链路可继续灰度回归。

## 3. 用户端测试结果

| 测试项 | 结果 | 证据 / 备注 |
| --- | --- | --- |
| 普通用户登录 | 通过 | 浏览器输入 `e2e_user` 后登录成功，进入用户端页面。 |
| token / 用户状态 | 通过 | 登录后显示 `E2E_TEST_普通用户`，刷新 `/questions` 后仍保持登录态。 |
| 退出登录 | 通过 | 点击头像菜单中的“退出登录”后跳转 `/login`。 |
| 未登录访问用户端 | 通过 | 退出后访问 `/questions` 跳转 `/login?redirect=/questions`。 |
| 工作台 | 通过 | `/dashboard` 可读，核心入口可点击，V2 能力标注“待接入”。 |
| 题库列表 | P1 问题 | `/questions` 真实打开但显示 `Total 0`，而错题/收藏与后台数据存在 E2E 题目。 |
| 题目详情 | 未测试 | 题库列表无真实行，未能从列表点击进入任意题目详情。 |
| 收藏题目 | 未测试 | 题库列表无真实行，未能执行收藏/取消收藏闭环。 |
| 错题记录 | 通过 | `/questions/wrong-records` 显示 2 条真实错题记录，可见“详情”“标记掌握”。 |
| 收藏题目列表 | 通过 | `/questions/favorites` 显示 1 条真实收藏记录，可见“详情”“取消收藏”。 |
| 简历列表 | P1 问题 | `/resumes` 显示简历总数 0，但创建面试页可选到 `E2E_TEST_Java后端三年经验简历（默认）`。 |
| 新增简历 | 通过 | 浏览器填写并保存测试简历，跳转到 `/resumes/100006/edit`，提示“简历已创建”。 |
| 编辑简历 | 通过 | 在编辑页修改简历名称并保存，刷新后输入框值保持为编辑后的内容。 |
| 删除简历 | 未测试 | 为避免误删非本轮数据，未在列表执行删除；本轮新建简历列表不可见，无法从列表确认删除入口。 |
| 设置默认简历 | 未测试 | 编辑页开关可见，但未执行设置默认，避免影响当前 E2E 默认简历。 |
| 项目经历 CRUD | P1 问题 | 新增项目弹窗可打开并填写，但点击“保存项目”后仍停留弹窗，页面仍显示“暂无项目经历”。 |
| 创建面试 | P1 问题 | 点击“开始面试”提示“面试已创建”，但仍停留创建页；历史中新增记录标题为默认 `CodeCoachAI V1 模拟面试`，未体现输入的自定义标题。 |
| 进入面试房间 | 通过 | 从历史点击第一条“房间”进入 `/interviews/room/100014`。 |
| 开始面试 / 当前题加载 | 通过 | 点击“开始面试”后状态变为等待作答，当前题真实加载。 |
| 提交答案 | 通过 | 输入真实答案并提交，页面状态更新为进行中。 |
| AI 评分展示 | 通过 | 提交后展示分数 `67` 与 AI 点评。 |
| AI 追问展示 | P1 问题 | 页面展示追问，但同时显示“原因：[fallback] 评分追问缺失或无效，使用本地兜底追问”，不符合禁止用前端兜底冒充真实 AI 结果的原则。 |
| 结束面试 | 通过 | 点击“结束面试”，确认弹窗 OK 后跳转报告页。 |
| 面试报告 | P1 问题 | `/interviews/100014/report` 显示总分 82 与报告内容，但“题目明细 0 条”，与已提交回答不一致。 |
| 面试历史 | 通过 | `/interviews/history` 可显示真实列表、分页、详情/房间/报告入口。 |

## 4. 管理端测试结果

| 测试项 | 结果 | 证据 / 备注 |
| --- | --- | --- |
| 管理员登录 | 通过 | 浏览器输入 `e2e_admin` 后登录成功，显示 `E2E_TEST_管理员` 和管理端入口。 |
| 管理后台首页 | 通过 | `/admin` 可访问；明确标注真实概览接口与演示图表占位。 |
| `GET /admin/system/overview` 指标 | P1 问题 | 页面显示接口可用，但用户数、题目数、简历数、面试数等均为 0，与后台列表真实数据明显不一致。 |
| 题目管理 | P1 问题 | `/admin/questions` 可访问但显示 `No Data / Total 0`，与题库、错题、历史数据存在真实题目不一致。 |
| 分类管理 | 部分通过 | `/admin/question-categories` 列表加载、分页可见；新增/编辑/删除未执行。 |
| 标签管理 | 通过 | `/admin/question-tags` 列表加载；已用测试标签执行新增与删除，删除确认弹窗正常。编辑因定位中断未完整验证。 |
| 题组管理 | P1 问题 | `/admin/question-groups` 可访问但显示 `Total 0`，而后端 E2E SQL 与历史面试数据存在题组。 |
| Prompt 管理 | 部分通过 | `/admin/ai/prompts` 列表、搜索区、分页、启禁用/删除入口可见；新增/编辑/删除未执行。 |
| AI 调用日志 | 通过 | `/admin/ai/logs` 显示 92 条真实调用日志，包含模型、场景、耗时、状态、失败原因、详情入口。 |
| 系统配置 | 部分通过 | `/admin/system/configs` 列表加载，编辑/删除入口可见；未修改配置，避免影响运行参数。 |
| 表格分页 | 部分通过 | 分类、标签、Prompt、AI 日志页面分页可见；只对标签列表执行过新增后刷新验证。 |
| 搜索筛选 | 部分通过 | 标签页面执行过关键词定位；其他后台核心页面未逐一提交搜索/重置。 |
| 弹窗 / 抽屉 | 部分通过 | 标签新增、删除确认弹窗可读；AI 日志详情抽屉、其他新增编辑弹窗未逐一验证。 |

## 5. 视觉交互测试结果

| 测试项 | 结果 | 备注 |
| --- | --- | --- |
| 登录页可读 | 通过 | 表单 label、placeholder、按钮可读。 |
| 用户端 Layout | 通过 | 侧边栏、顶部栏、主内容区可读。 |
| 管理端 Layout | 通过 | 管理端侧边栏、面包屑、顶部栏可读。 |
| 用户端侧边栏收缩 | 通过 | 点击 `Toggle sidebar` 后工作台仍可读。 |
| 管理端侧边栏收缩 | 未测试 | 未单独点击管理端收缩按钮。 |
| 顶部导航 | 通过 | 用户端与管理端顶部导航均可见。 |
| 用户头像下拉菜单 | 通过 | 可打开，含“修改密码”“退出登录”。 |
| 修改密码入口 | 通过 | 点击后进入 `/password`，页面可读。 |
| 深层路由刷新不白屏 | 通过 | 刷新 `/questions` 后保持登录态并正常渲染。 |
| Element Plus 表格 | 通过 | 已测错题、收藏、管理端多个表格，无明显白块。 |
| 表单输入 / placeholder | 通过 | 登录、简历、创建面试、标签弹窗均可读。 |
| 表单校验错误 | 未测试 | 未专门触发校验错误。 |
| 弹窗 | 通过 | 结束面试确认、标签删除确认、项目新增弹窗可读。 |
| 抽屉 | 未测试 | 未打开 AI 日志详情抽屉。 |
| 下拉框 / 日期选择器 | 未测试 | 下拉框可见但未逐项展开；日期选择器未覆盖。 |
| 分页 | 通过 | 管理端、历史列表分页控件可读。 |
| Loading / Empty | 部分通过 | Empty 状态可读；Loading 未专门截图验证。 |
| Message / Notification | 通过 | 登录、创建、保存、删除提示可读。 |
| Markdown / 代码块 | 部分通过 | 工作台代码块可读；题目详情 Markdown 未测。 |
| 窄屏 | 未测试 | 未切换窄屏 viewport。 |
| 滚动区域 / 固定布局遮挡 | 部分通过 | 已测主要页面滚动可用；未做全页面逐项覆盖。 |

## 6. 权限拦截测试结果

| 测试项 | 结果 | 证据 / 备注 |
| --- | --- | --- |
| 普通用户访问后台 | 通过 | 普通用户访问 `/admin` 后显示 403：“当前账号没有访问该页面的权限”。 |
| 管理员访问后台 | 通过 | 管理员可进入 `/admin`。 |
| 未登录访问后台 | 通过 | 退出后访问 `/admin` 跳转 `/login?redirect=/admin`。 |
| 未登录访问用户端 | 通过 | 退出后访问 `/questions` 跳转 `/login?redirect=/questions`。 |

## 7. 问题清单

| 编号 | 优先级 | 页面路径 | 复现步骤 | 实际结果 | 预期结果 | 初步判断 | 是否阻断后续 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| V2-BR-001 | P1 | `/questions` | 普通用户登录后打开题库列表 | 页面显示 `Total 0` | 应显示真实可用题库，至少与错题/收藏/后台数据一致 | 联调 / 数据 / 前端查询条件 | 是，阻断题目详情与收藏链路完整验证 |
| V2-BR-002 | P1 | `/resumes` | 普通用户打开简历中心 | 简历总数显示 0 | 应显示真实简历列表，包括创建面试页可选择的默认简历 | 联调 / 数据 / 前端列表解析 | 是，影响简历列表、删除和默认设置回归 |
| V2-BR-003 | P1 | `/resumes/100006/edit` | 打开项目经历弹窗，填写项目并点击“保存项目” | 弹窗未关闭，页面仍显示“暂无项目经历” | 应保存项目经历并刷新列表 | 前端 / 联调 | 是，阻断项目经历 CRUD |
| V2-BR-004 | P1 | `/interviews/create`、`/interviews/history` | 输入自定义面试名称后点击“开始面试”，再打开历史 | 创建成功但停留创建页；历史新增记录使用默认标题 `CodeCoachAI V1 模拟面试`，未体现自定义标题 | 创建成功后应进入房间或明确给出入口；历史应展示提交的标题或说明后端不支持该字段 | 前端 / 联调 / 数据 | 否，但影响体验和定位 |
| V2-BR-005 | P1 | `/interviews/room/100014` | 开始面试并提交答案 | AI 追问显示 `[fallback] 评分追问缺失或无效，使用本地兜底追问` | 后端未返回追问时应明确标注无追问或展示接口实际状态，不应用本地兜底追问冒充真实 AI 结果 | 前端 | 是，触碰“不伪造功能”原则 |
| V2-BR-006 | P1 | `/interviews/100014/report` | 提交答案并结束面试后查看报告 | 报告总分和总结可见，但“题目明细 0 条” | 应展示本场真实题目、回答、评分、点评明细，或明确说明后端未返回明细 | 联调 / 数据 / 前端解析 | 是，影响报告验收 |
| V2-BR-007 | P1 | `/admin` | 管理员打开管理首页 | 概览接口可用，但用户数、题目数、简历数、面试数、AI 调用数、Prompt 数均为 0 | 真实概览指标应与后台列表/真实数据一致，或标明接口未返回真实统计 | 后端 / 数据 / 联调 | 否 |
| V2-BR-008 | P1 | `/admin/questions` | 管理员打开题目管理 | 题目管理显示 `No Data / Total 0` | 应显示真实题目数据 | 联调 / 数据 / 前端查询条件 | 是，阻断题目管理 CRUD |
| V2-BR-009 | P1 | `/admin/question-groups` | 管理员打开题组管理 | 显示 `Total 0` | 应显示真实题组，或说明当前接口无数据 | 联调 / 数据 | 否 |
| V2-BR-010 | P2 | `/interviews/history` | 普通用户打开面试历史 | 部分旧数据显示 `Java ??`、`V1 ???? smoke` | 中文数据应正常显示，无乱码 | 数据 / 编码 | 否 |
| V2-BR-011 | P2 | `/dashboard`、`/admin` | 查看概览卡片 | 多个概览卡片显示 `--` 或 0，但列表页存在真实数据 | 缺失聚合字段应更明确区分“接口未返回”与“真实为 0” | 文案 / 联调 | 否 |
| V2-BR-012 | P2 | `/admin/question-tags` | 新增标签成功后观察弹窗 | 页面出现“标签已保存”，但弹窗残留文本曾短暂存在于页面底部区域 | 保存后弹窗应稳定关闭，不残留表单文案 | 样式 / 交互 | 否 |

## 8. 未测试项清单

1. 题目详情：题库列表无真实行，未从列表进入详情。
2. 收藏题目新增/取消闭环：题库列表无真实行，未执行。
3. 简历删除：本轮创建简历未在列表出现，未执行删除。
4. 设置默认简历：未执行，避免影响当前 E2E 默认简历。
5. 项目经历编辑/删除：新增项目未成功，无法继续编辑/删除。
6. 管理端题目新增/编辑/删除/启禁用：题目管理列表为空，未执行。
7. 管理端分类新增/编辑/删除/启禁用：仅验证列表与分页，未改数据。
8. 管理端题组新增/编辑/删除/关联题目：题组列表为空，未执行。
9. Prompt 新增/编辑/启禁用/删除：仅验证列表与入口，未改模板数据。
10. AI 调用日志详情抽屉：列表已验证，未打开详情抽屉。
11. 系统配置编辑保存：未修改运行配置，避免影响后端行为。
12. 管理端所有核心表格分页逐页点击：只覆盖了可见分页状态，未逐页全测。
13. 管理端所有核心搜索/重置/空结果：只覆盖了标签页面局部操作。
14. 表单校验错误样式：未专门触发。
15. 日期选择器暗色样式：本轮未遇到日期选择器页面。
16. 窄屏响应式：未切换移动/窄屏 viewport。

未测试项数量：16。

## 9. 后续建议

1. 优先排查用户题库、简历列表、管理题库、管理概览统计的接口返回与前端查询参数/解析是否一致。
2. 移除或调整面试房间中的本地兜底追问展示，确保未返回真实追问时明确展示真实接口状态。
3. 修复报告题目明细为空的问题，确认 report 接口是否返回 message/stage/detail 数据，前端只按真实字段展示。
4. 修复项目经历保存流程后，重新补测简历项目 CRUD。
5. 题库与后台题目管理恢复数据后，补测题目详情、收藏/取消收藏、题目管理 CRUD。
6. 在 P1 修复后再执行一次完整 7-B 浏览器回归，并补充未测试项。
