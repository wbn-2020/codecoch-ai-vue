# CodeCoachAI V2 前端第 7-B 浏览器真实全链路回归报告

## 1. 测试环境

| 项目 | 结果 |
| --- | --- |
| 前端仓库 | `C:\my-claude\CodeCoachAI-vue` |
| 前端分支 | `dev-ui` |
| 最新 commit | `048ba1e 小修复` |
| 工作区 | 开始前 `git status -sb` 为 `## dev-ui...origin/dev-ui`，无业务代码改动 |
| 前端启动地址 | `http://127.0.0.1:5173` |
| 后端地址 | `.env.development` 指向 `http://localhost:8080` |
| 测试时间 | 2026-05-16 21:20-21:55（Asia/Shanghai） |
| 浏览器 | Codex In-app Browser / Chromium |
| 普通用户账号 | `e2e_user`，密码未写入报告 |
| 管理员账号 | `e2e_admin`，密码未写入报告 |
| Mock 使用情况 | 未使用前端 Mock；页面与接口均连接本机真实后端 |

## 2. 开始前检查

| 检查项 | 结果 |
| --- | --- |
| `git branch --show-current` | `dev-ui` |
| `git status -sb` | `## dev-ui...origin/dev-ui` |
| `npm run build` | 通过 |
| lint/test 脚本 | `package.json` 仅有 `dev` / `build` / `preview`，无 lint/test 脚本 |
| 登录接口 | `POST http://localhost:8080/auth/login` 管理员账号返回 `code: 0`、真实 token 和用户角色 |
| 前端配置 | `.env.development` 为 `VITE_API_BASE_URL=http://localhost:8080` |

说明：构建出现 Rollup 注释 warning 与 chunk size warning，本轮按要求不处理 chunk warning。

## 3. 总体结论

- 存在 P0 阻断：2 个。
- 当前不建议进入下一阶段，原因是用户端题库列表和创建面试入口均出现真实浏览器空渲染，创建面试链路无法继续。
- 管理端可登录、后台布局可访问，多数管理路由可打开，但题目管理真实列表返回/展示为空，后台真实概览指标未正常落值，需要联调确认。
- 本轮只做测试记录；未修改 `src/api/**`、`src/router/**`、`src/stores/**`、`src/utils/request.ts`，未做 UI 重构，未新增依赖。

## 4. 用户端测试结果

| 测试项 | 结果 | 证据/说明 |
| --- | --- | --- |
| 普通用户登录 | 通过 | 浏览器输入 `e2e_user` 登录后进入 `/dashboard`，顶部显示 `E2E_TEST_普通用户` |
| token / 用户信息状态 | 通过 | 登录后刷新 `/dashboard` 仍保持用户态 |
| 退出登录 | 通过 | 点击头像菜单 `退出登录` 后跳转 `/login` |
| 未登录访问用户端 | 通过 | 退出后访问 `/dashboard` 跳转 `/login?redirect=/dashboard` |
| 工作台 | 通过 | `/dashboard` 可读，核心入口可见，V2 能力标注“待接入” |
| 题库列表 | 失败 P0 | `/questions` 浏览器真实访问后 `#app` 仅 `<!---->`，页面无文字 |
| 题目详情 | 通过 | `/questions/100000` 展示真实题干、参考答案、解析、分类、标签 |
| 提交题目答案 | 通过 | 在详情页输入答案并点击 `提交答案`，最近答题区域出现提交内容 |
| 收藏题目 | 部分失败 P1 | 点击 `取消收藏` 后可变为收藏态，但再次收藏并刷新后未恢复为 `取消收藏` |
| 错题记录 | 通过 | `/questions/wrong-records` 可打开，空状态和分页可读 |
| 收藏题目列表 | 通过 | `/questions/favorites` 可打开，空状态和分页可读 |
| 简历列表 | 通过 | `/resumes` 可打开，真实列表返回多条简历 |
| 新增简历 | 通过 | 浏览器填写表单并保存，跳转到 `/resumes/100005/edit`，提示“简历已创建” |
| 编辑简历 | 通过 | 创建后进入编辑页，表单数据保留，刷新仍可见 |
| 删除简历 | 失败 P1 | 简历列表卡片仅有“查看 / 编辑、创建面试、AI 优化、上传新版”，无删除入口；测试数据已通过真实接口清理 |
| 设置默认简历 | 未测试 | 本轮未切换默认简历，避免影响既有 E2E 默认数据 |
| 项目经历新增 | 通过 | 编辑页点击 `新增项目`，填写项目弹窗并保存后列表出现项目 |
| 项目经历编辑 | 通过 | 点击项目 `编辑`，修改项目名称后保存，页面显示新名称 |
| 项目经历删除 | 通过 | 点击项目 `删除`，确认弹窗为 `OK/Cancel`，确认后项目消失 |
| 创建面试 | 失败 P0 | `/interviews/create` 浏览器真实访问后 `#app` 仅 `<!---->`，页面无文字，无法提交字段 |
| 进入面试房间 | 未测试 | 创建面试页面阻断，未使用假 session 绕过 |
| 提交面试答案 | 未测试 | 创建面试页面阻断 |
| AI 评分展示 | 未测试 | 创建面试页面阻断 |
| AI 追问展示 | 未测试 | 创建面试页面阻断 |
| 结束面试 | 未测试 | 创建面试页面阻断 |
| 查看面试报告 | 通过 | `/interviews/100000/report` 可打开，明确声明内容来自 report 接口 |
| 面试历史 | 通过 | `/interviews/history` 可打开，空状态和分页可读 |

## 5. 管理端测试结果

| 测试项 | 结果 | 证据/说明 |
| --- | --- | --- |
| 管理员登录 | 通过 | 浏览器输入 `e2e_admin` 登录，随后可访问 `/admin` |
| 管理后台首页 | 部分失败 P1 | 页面可读，明确标注演示图表；真实概览指标显示 `--`，需要确认 `/admin/system/overview` 字段或加载状态 |
| 题目管理 | 失败 P1 | `/admin/questions` 页面可读，但列表 `No Data / Total 0`，与测试库存在 E2E 题目不一致 |
| 分类管理 | 通过 | `/admin/question-categories` 加载 15 条，分页可见 |
| 标签管理 | 通过 | `/admin/question-tags` 加载 11 条，新增弹窗可打开、表单可读 |
| 题组管理 | 通过 | `/admin/question-groups` 加载 10 条，编辑/删除入口可见 |
| Prompt 管理 | 通过 | `/admin/ai/prompts` 加载 13 条，场景、版本、状态字段可见 |
| AI 调用日志 | 通过 | `/admin/ai/logs` 加载 89 条，模型、场景、耗时、状态、失败原因字段可见 |
| 系统配置 | 部分失败 P1 | `/admin/system/configs` 页面可读，但列表 `No Data / Total 0`，与本地 E2E SQL 中配置项不一致 |
| 表格分页 | 部分通过 | 分类、标签、Prompt、AI 日志分页可见；题目管理和系统配置因数据为空无法完整验证分页刷新 |
| 搜索筛选 | 未测试 | 本轮未逐页执行查询、重置、空结果验证 |
| 新增/编辑/删除 | 部分测试 | 标签新增弹窗已打开验证；未提交管理端测试数据，避免误删关键数据 |
| 弹窗/抽屉 | 部分通过 | 标签新增弹窗可读；项目删除确认弹窗可交互；管理端详情抽屉未测试 |

## 6. 权限拦截测试结果

| 测试项 | 结果 | 证据/说明 |
| --- | --- | --- |
| 普通用户访问后台 | 通过 | 普通用户登录后访问 `/admin` 跳转 `/403` |
| 管理员访问后台 | 通过 | 管理员登录后可访问 `/admin` |
| 未登录访问后台 | 通过 | 退出后访问 `/admin` 跳转 `/login?redirect=/admin` |
| 未登录访问用户端 | 通过 | 退出后访问 `/dashboard` 跳转 `/login?redirect=/dashboard` |

## 7. 视觉交互与暗色主题结果

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 登录页可读 | 通过 | 深色登录页表单、按钮可读 |
| 用户端 Layout | 通过 | 侧栏、顶部栏、主体区域可读 |
| 管理端 Layout | 通过 | 管理端侧栏、面包屑、顶部栏可读 |
| 用户端侧边栏收缩 | 通过 | `Toggle sidebar` 可点击，布局未白屏 |
| 管理端侧边栏收缩 | 通过 | 管理端 `Toggle sidebar` 可点击，布局未白屏 |
| 顶部导航 | 通过 | 用户端和管理端顶部导航可读 |
| 用户头像下拉菜单 | 通过 | 可打开，含个人资料、修改密码、退出登录 |
| 修改密码入口 | 通过 | 菜单出现“修改密码”，入口可达性已确认 |
| 深层路由刷新 | 部分失败 P0 | `/questions/100000`、`/resumes`、`/interviews/100000/report` 可刷新；`/questions`、`/interviews/create` 空渲染 |
| Element Plus 表格白块 | 通过 | 管理端表格、分页整体暗色可读 |
| 表单输入/placeholder | 通过 | 登录、简历、项目弹窗、标签弹窗可读 |
| 表单校验错误 | 未测试 | 未专门提交空表单触发表单校验 |
| 弹窗 | 通过 | 项目新增/删除、标签新增弹窗可读 |
| 抽屉 | 未测试 | 未打开 AI 日志详情抽屉 |
| 下拉框 | 通过 | 难度、状态、分类等 Select 可见 |
| 日期选择器 | 未测试 | 本轮未遇到日期选择器 |
| 分页 | 通过 | 管理端和用户端表格分页可读 |
| Loading 状态 | 未测试 | 未捕获明显 loading 中间态 |
| Empty 状态 | 通过 | 错题、收藏、面试历史等空状态可读 |
| Message / Notification | 通过 | 简历创建、答案提交、项目保存/删除提示可见 |
| Markdown | 通过 | 题目详情参考答案/解析可读 |
| 代码块 | 通过 | 工作台 code 状态块可读 |
| 窄屏 | 未测试 | 未切换移动端视口 |
| 滚动区域 | 通过 | 简历编辑和管理端长表格滚动未遮挡主要操作 |

## 8. 问题清单

| 编号 | 优先级 | 页面路径 | 复现步骤 | 实际结果 | 预期结果 | 初步判断 | 是否阻断后续 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| V2B-001 | P0 | `/questions` | 普通用户登录后浏览器访问题库列表 | 页面标题为“题库”，但 `#app` 渲染为 `<!---->`，页面无内容 | 应展示题库列表、搜索、筛选、分页和真实数据 | 前端 | 是 |
| V2B-002 | P0 | `/interviews/create` | 普通用户登录后浏览器访问创建面试页 | 页面标题为“创建面试”，但 `#app` 渲染为 `<!---->`，页面无内容 | 应展示创建面试表单并可提交真实字段 | 前端 | 是 |
| V2B-003 | P1 | `/resumes` | 新增测试简历后回到简历中心 | 简历卡片无删除按钮，无法按页面完成删除简历测试 | 应提供删除入口或明确说明删除能力不可用 | 前端 | 否 |
| V2B-004 | P1 | `/questions/100000` | 点击取消收藏后再次收藏并刷新 | 刷新后未恢复为 `取消收藏` 状态 | 收藏/取消收藏状态应持久化并与刷新后一致 | 联调 | 否 |
| V2B-005 | P1 | `/admin/questions` | 管理员登录后访问题目管理 | 页面显示 `No Data / Total 0` | 应展示本地 E2E 题库数据或给出明确空数据原因 | 联调/数据 | 否 |
| V2B-006 | P1 | `/admin` | 管理员访问后台首页 | 真实概览指标均为 `--`，长期显示概览接口连接中语义 | `/admin/system/overview` 成功时应展示真实指标或失败态 | 联调 | 否 |
| V2B-007 | P1 | `/admin/system/configs` | 管理员访问系统配置 | 页面显示 `No Data / Total 0` | 应展示本地 E2E 配置项或明确无数据原因 | 联调/数据 | 否 |
| V2B-008 | P2 | 项目经历删除确认弹窗 | 点击项目删除 | 确认按钮显示英文 `OK/Cancel` | 暗色中文产品界面中建议统一为中文确认文案 | 样式/文案 | 否 |

## 9. 未测试项清单

1. 设置默认简历：未测试，避免影响既有 E2E 默认数据。
2. 创建面试字段提交：未测试，`/interviews/create` 空渲染阻断。
3. 进入新建面试房间：未测试，创建面试阻断。
4. 新建面试提交答案：未测试，创建面试阻断。
5. 新建面试 AI 评分展示：未测试，创建面试阻断。
6. 新建面试 AI 追问展示：未测试，创建面试阻断。
7. 新建面试结束面试：未测试，创建面试阻断。
8. 新建面试报告生成/重试：未测试，创建面试阻断。
9. 管理端题目新增/编辑/删除/启禁用：未测试，仅验证页面加载异常。
10. 管理端分类新增/编辑/删除/启禁用：未测试，已验证列表加载。
11. 管理端标签新增/编辑/删除/启禁用提交：未测试，仅打开新增弹窗。
12. 管理端题组新增/编辑/删除：未测试，已验证列表加载。
13. Prompt 新增/编辑/启禁用/删除：未测试，已验证列表加载。
14. AI 日志详情抽屉：未测试。
15. 系统配置编辑保存：未测试，列表为空。
16. 管理端核心页面搜索筛选、重置、空结果：未测试。
17. 日期选择器暗色可读性：未测试。
18. 表单校验错误态：未测试。
19. Loading 状态：未测试。
20. 窄屏移动端布局：未测试。

## 10. 后续建议

1. 优先修复 `/questions` 和 `/interviews/create` 空渲染，这两个属于进入下一阶段前的 P0。
2. 修复后重新执行用户端题库列表、创建面试、面试房间、答题、评分、追问、结束、报告的完整浏览器链路。
3. 联调确认 `/admin/questions`、`/admin/system/overview`、`/admin/system/configs` 的真实接口返回和前端字段解析。
4. 为简历中心补齐删除入口，或在 UI 中明确标注删除能力不可用；当前需求要求简历删除可测。
5. 对收藏接口进行一次请求/响应级复核，确认刷新后状态来源字段是否与详情页状态解析一致。
6. 下一轮回归应补齐管理端 CRUD 提交类测试，但只使用 `E2E_BROWSER_*` 前缀测试数据并在结束后清理。

## 11. 结束后检查

| 检查项 | 结果 |
| --- | --- |
| 测试数据清理 | 新增简历 `E2E_BROWSER_20260516133148` 已通过真实 `DELETE /resumes/100005` 接口清理成功 |
| 业务代码修改 | 无 |
| 报告文件 | `docs/V2_frontend_browser_regression_report.md` |
